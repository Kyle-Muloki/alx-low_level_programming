#include <stdio.h>
/**
 * main - this is the function that introduces other functions
 * Return: 0 when its complete
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
